---
name: ieee-massive-search
description: Use when user provides an IEEE Xplore search URL and wants to extract all paper metadata (title, arnumber, year, etc.) to a Markdown file with automatic pagination
---

# IEEE Massive Search

批量提取IEEE Xplore搜索结果为Markdown清单。

## When to Use

- 用户提供IEEE Xplore搜索URL，需要提取全部结果
- 需要获取论文的arnumber/DOI用于后续分析
- 搜索结果超过100篇，需要自动翻页

## Quick Reference

| 参数 | 说明 |
|------|------|
| `--url` | IEEE搜索URL |
| `--output` | 输出文件路径 (.md/.json/.csv) |
| `--max-pages` | 限制最大页数（可选） |
| `--visible` | 可视化模式调试 |

## Usage

```bash
# 激活环境
conda activate ieee-extractor

# 基本用法（使用环境变量）
python ${CLAUDE_SKILL_DIR}/ieee-massive-search/scripts/ieee_url_extractor.py \
  --url "IEEE_SEARCH_URL" \
  --output "results.md"

# 限制页数（大数据量预览）
python ${CLAUDE_SKILL_DIR}/ieee-massive-search/scripts/ieee_url_extractor.py \
  --url "..." --output "preview.md" --max-pages 2

# JSON输出
python ${CLAUDE_SKILL_DIR}/ieee-massive-search/scripts/ieee_url_extractor.py \
  --url "..." --output "results.json"
```

## Output Format

Markdown表格：
```markdown
| # | Article Number | 标题 | 年份 | 出版物 | 被引 | URL |
|---|----------------|------|------|--------|------|-----|
| 1 | 12345678 | Paper Title... | 2024 | IEEE JSSC | 15 | [Link](...) |
```

## Workflow Integration

```
┌─────────────────────────────────────┐
│  1. ieee-advanced-search 生成URL    │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│  2. ieee-massive-search 提取清单    │
│     → 输出 Markdown 文件            │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│  3. ieee-researcher 分析清单        │
│     → 读取 arnumber 列              │
│     → 逐篇调用 ieee-paper-detail   │
└─────────────────────────────────────┘
```

## Implementation

脚本位置: `${CLAUDE_SKILL_DIR}/ieee-massive-search/scripts/ieee_url_extractor.py`

**调用方式**:
```bash
# 激活环境
conda activate ieee-extractor

# 运行脚本（使用环境变量路径）
python ${CLAUDE_SKILL_DIR}/ieee-massive-search/scripts/ieee_url_extractor.py \
  --url "IEEE_SEARCH_URL" \
  --output "/mnt/d/obsidian/03-Projects/项目名/文档/ieee-urls/results.md"
```

依赖环境:
```bash
conda create -n ieee-extractor python=3.11 -y
conda activate ieee-extractor
pip install playwright
playwright install chromium
```

## Common Issues

| 问题 | 解决方案 |
|------|----------|
| 超时错误 | 重试，或使用 `--visible` 检查验证码 |
| 0结果 | 检查URL编码，特殊字符需URL encode |
| 验证码阻塞 | 使用 `--visible` 模式手动处理 |

## Related Skills

- `ieee-advanced-search` — 构建搜索URL
- `ieee-paper-detail` — 提取单篇论文详情
- `ieee-researcher` — 批量分析论文